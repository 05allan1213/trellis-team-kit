Continue working on the active Trellis task. Detect the current task using `task.py current --source`, load the task context (prd.md, design.md, implement.md, review results), and resume from the appropriate workflow state.

If no active task exists, ask the user which task to continue.

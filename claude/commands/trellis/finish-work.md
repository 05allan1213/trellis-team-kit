Finish the active Trellis task. Load the trellis-finish-work skill and execute the finish-work workflow: verify implementation complete, `trellis-check` PASS, `validate_review_gates.py <task-dir>` PASS (selected reviews have concrete artifacts, no placeholders, and matching PASS reviewer agent results), the current `finish.md` template is complete, required merge-review PASS, code committed or explicitly not needed, and `validation/test-results.md` records Build/Test/Smoke, Ready for finish-work?, and Overall PASS or skipped-with-valid-reason evidence. Then run `python3 ./.trellis/scripts/finalize_task_archive.py <task-dir>` to archive and sync the workspace journal/index.

Do NOT write new code. Do NOT fix bugs. Do NOT bypass failed gates.

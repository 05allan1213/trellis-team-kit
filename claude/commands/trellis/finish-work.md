Finish the active Trellis task. Load the trellis-finish-work skill and execute the finish-work workflow: verify all gates passed, spec update decision recorded, code committed, then run `python3 ./.trellis/scripts/finalize_task_archive.py <task-dir>` to archive and sync the workspace journal/index.

Do NOT write new code. Do NOT fix bugs. Do NOT bypass failed gates.
